<h1>Login</h1>

<%= form_tag(:login, :method => 'post') do %>
	<div>
		<%= label_tag(:user, "User:") %>
		<%= text_field_tag(:user) %>
	</div>
	<div>
		<%= label_tag(:password, "Pass:") %>
		<%= password_field_tag(:password) %>
	</div>
	<%= submit_tag("Login") %>
<% end %>
